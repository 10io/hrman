<%= render(:partial => "form", :layout => "modal", :locals => { :action => "Edit" }) %>
<script>
  $(function() {
    $('#modal').modal('show').off('hidden.bs.modal').on('hidden.bs.modal', function() {
      Turbolinks.visit('<%= employees_url %>')
    })
  })
</script>
