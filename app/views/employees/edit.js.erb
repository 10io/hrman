$('#modal').replaceWith('<%= j render(:partial => "form", :layout => "modal", :locals => { :action => "Edit" }) %>')
$('#modal').modal('show').off('hidden.bs.modal').on('hidden.bs.modal', function() {
  history.back()
})
history.pushState(null, null, '<%= edit_employee_url(@employee) %>')
